# Dockerfile
# Образ для запуска модуля sensors

# Используем минимальный образ Python
FROM python:3.10-slim

# Устанавливаем рабочую директорию
WORKDIR /app

# Копируем скрипт
COPY sensors.py .

# Устанавливаем зависимости (в нашем случае все из стандартной библиотеки, ничего не требуется)
ENV PYTHONUNBUFFERED=1
# Указываем команду запуска контейнера
CMD ["python", "sensors.py"]
